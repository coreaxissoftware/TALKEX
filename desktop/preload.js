// Intentionally minimal — the web app runs unchanged inside the window. A
// preload exists only so contextIsolation is on (Electron security best
// practice) without exposing any Node APIs to the page.
